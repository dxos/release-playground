{
  "name": "release-playground",
  "version": "1.0.0",
  "private": true,
  "description": "playground monorepo.",
  "repository": "git@github.com:dxos/release-playground.git",
  "license": "AGPLv3",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "yarn lerna run build",
    "test": "yarn lerna run test"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  }
}
